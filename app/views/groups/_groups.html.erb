
<ul>
  <% @groups.each do |group| %>
    <li><%= link_to group.name, group_path(group) %> - <%= link_to "Edit", edit_group_path(group) %> | <%= link_to "Delete", group_path(group), method: :delete %></li>
  <% end %>
</ul>